services:
  base:
    image: exilesprx/blockchain:base
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: base

  compile:
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: compile
