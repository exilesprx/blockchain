node_modules/
.pnpm-store/
build/
.vscode
*.http
.env
.env.eventstore
.env.graylog
coverage/
.dccache

# TypeScript build info (not needed since we use tsx runtime)
*.tsbuildinfo

# Workspace dist directories (if we build in the future)
packages/*/dist/