describe("Transactions", () => {

    test("it expects a transaction can occur between different wallets", () => {
        
    });

    test("it expects a transaction should fail between the same wallet", () => {
        
    });
});